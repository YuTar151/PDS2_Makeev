"""
2, Напишіть програму, яка б приймала список з числами та перевіряла чи всі числа в ньому унікальні,
Реалізуйте у функції декілька обробок виключень,
"""
list = [1, 2, 7, 6, '8, 3, 2, 5]
try:
    set_list = set(list)
    if len(list) == len(set_list):
        print("Всі числа в списку унікальні")
    else:
        print("В списку є дубльовані числа")

except SyntaxError as ex:
    print("Не вірно задані данні в списку")
    print(ex)








